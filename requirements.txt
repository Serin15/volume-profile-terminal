# Volume Profile Platform - dependinte
# Instalare: python -m venv .venv && .venv\Scripts\pip install -r requirements.txt

# Date: incarcare rapida + Parquet
pandas
pyarrow

# Vizualizare
matplotlib
plotly
streamlit

# Backend platforma (Faza 2)
fastapi
uvicorn

# Desktop app (GUI nativ, real-time)
PySide6
pyqtgraph

# Fus orar / DST (zoneinfo pe Windows NU are baza de fusuri fara asta -> granita sesiunii
# DST-aware, filtrul "Doar RTH" si umbrirea sesiunii ar crapa pe un PC nou fara tzdata)
tzdata

# Teste
pytest

# Packaging .exe (optional, doar pt build PyInstaller - vezi MIGRATION.md): pyinstaller
