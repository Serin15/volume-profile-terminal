# Mediu virtual
.venv/

# Cache Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Output generat
output/

# Date brute CSV (mari, ~767MB, regenerabile din Databento) - NU se urca pe GitHub.
data/raw/
# NOTA: data/parquet/ (mic, ~55MB, fisiere <5MB) SE versioneaza intentionat ->
# pe un PC nou datele vin automat cu `git clone` (fara copiere manuala).

# Streamlit (prototip vechi)
.streamlit/

# Build PyInstaller (artefacte, regenerabile din run_desktop.py - NU se comit)
build/
dist/
*.spec

# IDE
.idea/
