# VP Terminal — HANDOFF pentru chat nou (2026-08-18)

> Lipește acest fișier în chat-ul nou ca să ai tot contextul. Continuăm de aici.

## 0. Proiectul
Terminal desktop **Volume Profile + Order Flow** pentru NQ (Python, PySide6 + pyqtgraph, date Databento), în **`D:\Volume Profile`**. Uz personal (Ali = **scalper pe 1min**), backtesting + învățat order flow. Repo PRIVAT: `github.com/Serin15/volume-profile-terminal`. `gh` NU e instalat (PR-uri se deschid din browser). Rulare: `.venv\Scripts\python.exe run_desktop.py`. Teste: `.venv\Scripts\python.exe -m pytest -q` → **267 passed** (~13 min).

**Regula de aur:** tool de **analiză de context**, ZERO BUY/SELL/semnale.

## 1. Starea Git (IMPORTANT)
- **`main` = `b3217da`** — neatins toată sesiunea asta (Context Engine înghețat).
- **`feat/ux-polish`** = branch-ul ACTIV, stacked pe `feat/profile-only`, conține **TOT ce am făcut azi**. Pushed. **Nimic pe main. PR-uri pending** (merge din browser: `compare/main...feat/ux-polish`).
- Ca să vezi tot: `git checkout feat/ux-polish` + rulează app-ul. Reversibil oricând.

## 2. Ce am făcut azi (pe feat/ux-polish, commit-uri separate, 267 teste verzi)
1. **UX polish** — pastile de nivel închise (nu cutii rainbow), VA slate ≠ VWAP portocaliu, panoul Context arată **direcția** („Coherent ▲ bullish / ▼ bearish", nu verde-orbește), grid heatmap temperat, bara de straturi grupată cu etichete (PROFIL/ORDER FLOW/SESIUNE).
2. **Profile Only** — mod nou: câte un VP per sesiune/zi de-a lungul timpului, fără lumânări (stil DeepCharts). Selector Zi/Asia/Londra/NY/Toate.
3. **4 fix-uri order-flow pentru 1min** (tool-ul era calibrat pt 5min → pe 1min zgomot):
   - Markere absorption/exhaustion **interval-aware** (`_detector_defaults` în data_service): 1min de la ~35 exh/zi la ~5, fiecare real. 5min NESCHIMBAT.
   - **POC migration** → POC pe fereastră mobilă (`_rolling_poc_series`): de la 96% SIDEWAYS (inutil) la ~46% informativ.
   - **Delta FLIP** cere `|prev| >= neutral` (ambele bare semnificative): 36% → 26%.
   - **Panoul abs/exh** interval-aware (`_ctx_config_for_interval` în main.py, ContextEngine recreat la schimbare interval).
   - CVD divergence + LVN verificate empiric → **OK, lăsate** (cvd ~21% = divergențe reale; marirea bratelor a înrăutățit-o).
4. **Acceptance/Rejection markers** (feature nou) — **inele** la marginile Value Area (verde=bullish, mov=bearish), `detect_level_reactions` în data_service, filtrat (~25/sesiune pe 1min, nu tapet). Toggle „Acc/Rej". + **finisaj premium** abs/exh (glow + contur crisp), formele păstrate (triunghi/romb).

## 3. Livrabile (Desktop\VP-AUDIT\)
- `BACKTESTING-PLAYBOOK.md` — metodologie ca-la-carte (LOCAȚIE→CONTEXT→TRIGGER, top-down, commit-before-reveal, tabel per-tool, plan săptămâna 1-3).
- `REPLAY-STUDY-PROTOCOL.md` — protocol de studiu replay.
- `UX-IMPROVEMENTS.md` — lista UX prioritizată (mai rămâne rail-ul de studii).
- `DOCUMENTATIE-COMPLETA.md` — doc tehnic complet al tool-ului.
- `manual.html` + `manual-img/` — MANUALUL de utilizare (vezi §4).

## 4. MANUALUL DE UTILIZARE (în lucru — DOAR MODELUL e gata)
- Publicat ca **Artifact (pagină web)**: **https://claude.ai/code/artifact/79e20c17-dba2-4f63-b006-c7dfbdd54299**
- **Făcut:** Orientare + Limbajul culorilor + Harta ecranului (screenshot cu 10 callout-uri) + Quick Start + **Secțiunea 1 · Bara de sus** (formatul de referință: per control → Ce e / Cum / De ce, cu poză).
- **RĂMAS (în același format):** 2·Straturi, 3·Order Flow (markere), 4·Volume Profile, 5·Replay/Backtesting, 6·Desen, 7·Panoul Context, 8·Profile Only, + **Glosar**.
- **Cum se continuă:** editezi `manual.html`, injectezi poze noi, republici cu `url=` linkul de mai sus (păstrează același link).
- **TRUC capturi curate offscreen** (altfel textul e pătrățele): în scriptul de grab încarcă fonturile Windows — `QtGui.QFontDatabase.addApplicationFont(r"C:\Windows\Fonts\segoeui.ttf")` (+ arial.ttf, seguisym.ttf pt ⚙/📂) apoi `app.setFont(QFont("Segoe UI",10))`. Capturi pe zone: `win.findChild(QtWidgets.QWidget, "TopBar"/"LayerBar"/"ReplayBar").grab().save(...)`. Vezi `scratchpad/shots.py`.

## 5. TODO / decizii deschise
- **Manualul:** de completat secțiunile 2-8 + glosar (prioritatea, urmează să-l dea unui tester).
- **Verificare vizuală pe 1min de Ali** — offscreen confirmă, dar el trebuie să se uite pe display real (vederea „Order Flow" aprinde toate markerele).
- **Merge `feat/ux-polish` → main** când Ali e mulțumit (aduce tot).
- **Task DST** (task_f6956520): granița zilei 22:00 UTC hardcodată în loader.py — de reparat (DST-aware via zoneinfo) înainte de date de iarnă.
- **Rail vertical de „studii"** (restructurarea mare a controalelor) — amânată, cere testare live.

## 6. Preferințe Ali
Pas-cu-pas, în română, validare pe fiecare pas. Vrea nivel DeepCharts/ATAS/Sierra. Se pierde în multe feature-uri → de-aia manualul. Prefer o concluzie corectă în locul uneia care sună bine.
